.DS_Store
.env
/node_modules/

# Original full-resolution photo — the optimized copies actually used by
# the site live in public/images/.
/aaron_photo.jpeg

# React Router
/.react-router/
/build/

# Generated architecture diagram (archify) — a standalone project
# artifact for reference, not site content, so it isn't committed.
/diagrams/

# Local tool state (impeccable design-skill config/hooks) — machine-local,
# not project source. .impeccable/surfaces/ (the direction contract) and
# PRODUCT.md/DESIGN.md at the repo root are real project docs and stay
# tracked.
/.impeccable/config.local.json
/.codex/
